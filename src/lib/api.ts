/**
 * Backwards-compatible API facade.
 *
 * New code should import from the focused modules under ./api when possible.
 * Existing imports from ./api continue to work through these re-exports.
 */
export * from './api/index';
